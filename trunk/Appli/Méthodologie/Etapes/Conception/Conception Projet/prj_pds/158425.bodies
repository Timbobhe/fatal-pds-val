class Transformer
!!!159321.java!!!	transformToBinary(in s : String) : byte
		byte[] tab = new byte[s.length()];
		byte x;
		for(int i = 0; i < s.length(); i++){
			x = Byte.parseByte(s.charAt(i)+"");
			tab[i] = x;
		}
		return tab;	
